function atom3(MBC){;
  document.write("이곳은 atom3 함수 안이다." + (MBC+300) + "<br/>");

  console.log("이곳은 atom3 함수 안이에용" + (100+200));

  alert("이곳은 atom3 함수 안이에요~" + (100+150)); 
}

function atom4(MBC){;
  document.write("이곳은 atom3 함수 안이다." + (MBC+300) + "<br/>");

  console.log("이곳은 atom3 함수 안이에용" + (100+200));

  alert("이곳은 atom3 함수 안이에요~" + (100+150)); 
}